USE [db_app_server]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW [V].[ENTITY_NOT_LOCKED] AS
SELECT * FROM A.ENTITY
WHERE IdObject NOT IN 
(SELECT IdObject FROM A.ENTITY_LOCKED WHERE CodeEntity='entity')

GO

