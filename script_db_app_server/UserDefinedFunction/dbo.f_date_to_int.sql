USE [db_app_server]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE FUNCTION [dbo].[f_date_to_int] ( @Date  DATETIME ) RETURNS INT AS BEGIN 

	RETURN CONVERT( INT, (CONVERT(VARCHAR, @Date, 112)))

END

GO

